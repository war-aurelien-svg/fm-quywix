"use client";
import { useState } from "react";
import { ArrowDownLeft, ArrowUpRight, ChevronLeft, Plus } from "lucide-react";
const seasons = { "2025-2026": { period: "Saison 2025 — 2026", arrivals: [] as string[][], exits: [] as string[][] } } as const;
function Transfer({ item, outbound }: { item: readonly string[]; outbound?: boolean }) { return <article className="flex items-center gap-3 rounded-2xl border border-white/[.07] bg-white/[.025] p-4"><div className={`grid h-11 w-11 place-items-center rounded-2xl ${outbound ? "bg-rose-400/15 text-rose-300" : "bg-emerald-400/15 text-emerald-300"}`}>{outbound ? <ArrowUpRight size={19}/> : <ArrowDownLeft size={19}/>}</div><div className="min-w-0 flex-1"><h3 className="font-semibold">{item[0]}</h3><p className="mt-1 text-xs text-slate-500">{outbound ? "Vers " : "Depuis "}{item[1]} · {item[3]}</p></div><div className="text-right"><p className="font-semibold">{item[2]}</p><p className="mt-1 text-[10px] text-slate-500">{item[4]}</p></div></article> }
export default function MercatoPage() { const [season, setSeason] = useState<keyof typeof seasons>("2025-2026"); const data = seasons[season]; const expenses = data.arrivals.reduce((sum, x) => sum + Number(x[2].replace(" M€", "")), 0); const income = data.exits.reduce((sum, x) => sum + Number(x[2].replace(" M€", "")), 0); return <main className="min-h-screen bg-[radial-gradient(circle_at_85%_0,rgba(44,104,255,.16),transparent_30%),#090b10] px-4 py-6 text-slate-100 sm:px-8 lg:px-14"><a href="/" className="mb-8 inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ChevronLeft size={16}/>Retour au tableau de bord</a><header className="mb-8 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-electric">{data.period}</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Mercato</h1><p className="mt-2 text-sm text-slate-400">Les mouvements de la saison {season}.</p></div><div className="flex gap-3"><select value={season} onChange={e => setSeason(e.target.value as keyof typeof seasons)} className="rounded-xl border border-white/10 bg-white/[.04] px-3 text-sm font-semibold outline-none"><option className="bg-panel" value="2025-2026">2025 — 2026</option></select><button className="inline-flex items-center gap-2 rounded-xl bg-electric px-4 py-2.5 text-sm font-semibold"><Plus size={16}/>Ajouter un transfert</button></div></header><div className="mb-5 grid gap-4 sm:grid-cols-3"><div className="surface p-5"><p className="muted">Dépenses</p><p className="mt-2 text-2xl font-semibold text-rose-300">{expenses} M€</p></div><div className="surface p-5"><p className="muted">Recettes</p><p className="mt-2 text-2xl font-semibold text-emerald-300">{income} M€</p></div><div className="surface p-5"><p className="muted">Balance</p><p className="mt-2 text-2xl font-semibold">{income - expenses >= 0 ? "+" : ""}{income - expenses} M€</p></div></div><div className="grid gap-5 lg:grid-cols-2"><section className="surface p-5 sm:p-6"><div className="mb-5 flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-400/15 text-emerald-300"><ArrowDownLeft size={17}/></span><div><h2 className="font-semibold">Arrivées</h2><p className="muted">{data.arrivals.length} recrues</p></div></div><div className="space-y-3">{data.arrivals.map(x=><Transfer key={x[0]} item={x}/>)}</div></section><section className="surface p-5 sm:p-6"><div className="mb-5 flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-rose-400/15 text-rose-300"><ArrowUpRight size={17}/></span><div><h2 className="font-semibold">Départs</h2><p className="muted">{data.exits.length} joueurs</p></div></div><div className="space-y-3">{data.exits.map(x=><Transfer key={x[0]} item={x} outbound/>)}</div></section></div></main>; }
