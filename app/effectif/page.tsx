"use client";
import { useState } from "react";
import { ArrowUpRight, Filter, Search, SlidersHorizontal, Star, TrendingUp } from "lucide-react";
import { ClubHeader } from "../../components/club-header";

const seasons = { "2025-2026": { club: "LOSC Lille", total: "0", value: "—", description: "Importez une sauvegarde pour afficher les joueurs.", players: [] as string[][] } } as const;
const colors = ["from-blue-400 to-indigo-800","from-amber-300 to-orange-700","from-teal-300 to-cyan-800","from-violet-300 to-purple-800","from-rose-300 to-red-800","from-lime-300 to-emerald-800"];
function Avatar({ name, index }: { name: string; index: number }) { return <div className={`grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-gradient-to-br ${colors[index]} text-sm font-bold`}>{name.split(" ").map(x => x[0]).join("")}</div>; }
export default function EffectifPage() { const [season, setSeason] = useState<keyof typeof seasons>("2025-2026"); const data = seasons[season]; return <main className="min-h-screen bg-[radial-gradient(circle_at_90%_0,rgba(44,104,255,.16),transparent_30%),#090b10] px-4 py-6 text-slate-100 sm:px-8 lg:px-14"><ClubHeader active="Effectif"/><header className="mb-8 mt-8 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-electric">{data.club} · {season}</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Effectif</h1><p className="mt-2 text-sm text-slate-400">{data.description}</p></div><div className="flex flex-col gap-3 sm:flex-row"><label className="flex items-center gap-3 rounded-xl border border-white/10 bg-white/[.04] px-3 py-2 text-sm"><span className="text-slate-400">Saison</span><select value={season} onChange={e => setSeason(e.target.value as keyof typeof seasons)} className="bg-transparent font-semibold outline-none"><option className="bg-panel" value="2025-2026">2025 — 2026</option></select></label><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-electric px-4 py-2.5 text-sm font-semibold shadow-glow"><TrendingUp size={16}/>Comparer les joueurs</button></div></header><div className="mb-5 grid gap-4 sm:grid-cols-3"><section className="surface p-5"><p className="muted">Joueurs inscrits</p><p className="mt-2 text-2xl font-semibold">{data.total}</p></section><section className="surface p-5"><p className="muted">Valeur estimée</p><p className="mt-2 text-2xl font-semibold">{data.value}</p></section><section className="surface p-5"><p className="muted">Moyenne de forme</p><p className="mt-2 text-2xl font-semibold">7,7 <span className="text-sm text-emerald-400">+ 0,4</span></p></section></div><section className="surface overflow-hidden"><div className="flex flex-col gap-3 border-b border-white/[.07] p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2 rounded-xl border border-white/10 bg-black/15 px-3 py-2"><Search size={16} className="text-slate-500"/><input className="w-48 bg-transparent text-sm outline-none placeholder:text-slate-500" placeholder="Rechercher un joueur"/></div><div className="flex gap-2"><button className="rounded-xl border border-white/10 p-2.5 text-slate-400"><Filter size={16}/></button><button className="rounded-xl border border-white/10 p-2.5 text-slate-400"><SlidersHorizontal size={16}/></button></div></div><div className="divide-y divide-white/[.07]">{data.players.map(([name, role, age, country, value, rating, progress], i) => <article key={name} className="group flex items-center gap-3 p-4 transition hover:bg-white/[.035] sm:gap-5 sm:px-6"><Avatar name={name} index={i}/><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h2 className="truncate text-sm font-semibold sm:text-base">{name}</h2><span className="rounded-md bg-electric/15 px-1.5 py-0.5 text-[10px] font-bold text-blue-300">{role}</span></div><p className="mt-1 text-xs text-slate-500">{country} · {age} · Contrat jusqu’en {Number(season.slice(0,4)) + 3}</p></div><div className="hidden text-right sm:block"><p className="text-sm font-semibold">{value}</p><p className="mt-1 text-xs text-emerald-400">Progression {progress}</p></div><div className="rounded-xl bg-amber-300/10 px-3 py-2 text-center"><p className="flex items-center gap-1 text-sm font-bold text-amber-200"><Star size={12} fill="currentColor"/>{rating}</p><p className="mt-0.5 text-[9px] uppercase tracking-wider text-slate-500">forme</p></div><ArrowUpRight size={18} className="text-slate-600 transition group-hover:text-white"/></article>)}</div></section></main>; }
