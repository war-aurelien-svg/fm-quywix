import { ChevronLeft, ChevronRight, Sparkles } from "lucide-react";
type Item = { title: string; text: string; badge?: string }; type Metric = { label: string; value: string; trend?: string };
export function ModulePage({ eyebrow, title, description, metrics, items, action }: { eyebrow: string; title: string; description: string; metrics: Metric[]; items: Item[]; action?: string }) { return <main className="min-h-screen bg-[radial-gradient(circle_at_85%_0,rgba(44,104,255,.16),transparent_30%),#090b10] px-4 py-6 text-slate-100 sm:px-8 lg:px-14"><a href="/" className="mb-8 inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ChevronLeft size={16}/>Retour au tableau de bord</a><header className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-electric">{eyebrow}</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">{title}</h1><p className="mt-2 max-w-xl text-sm text-slate-400">{description}</p></div>{action && <button className="inline-flex items-center gap-2 rounded-xl bg-electric px-4 py-2.5 text-sm font-semibold shadow-glow"><Sparkles size={16}/>{action}</button>}</header><div className="mb-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(m=><section className="surface p-5" key={m.label}><p className="muted">{m.label}</p><p className="mt-2 text-2xl font-semibold">{m.value}</p>{m.trend && <p className="mt-2 text-xs text-emerald-400">{m.trend}</p>}</section>)}</div><section className="surface overflow-hidden"><div className="flex items-center justify-between border-b border-white/[.07] px-5 py-4"><div><h2 className="font-semibold">À suivre</h2><p className="mt-1 text-xs text-slate-500">Les éléments importants de votre saison</p></div><ChevronRight size={18} className="text-slate-500"/></div><div className="divide-y divide-white/[.07]">{items.map((item, index)=><article key={item.title} className="flex items-center gap-4 p-5 hover:bg-white/[.025]"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-electric/15 text-xs font-bold text-blue-300">{String(index + 1).padStart(2,"0")}</span><div className="min-w-0 flex-1"><h3 className="text-sm font-semibold">{item.title}</h3><p className="mt-1 text-sm text-slate-400">{item.text}</p></div>{item.badge && <span className="rounded-lg bg-white/[.07] px-2 py-1 text-xs font-medium text-slate-300">{item.badge}</span>}</article>)}</div></section></main>; }
